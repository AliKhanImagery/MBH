{
  "mcpServers": {
    "gpt-image-1": {
      "command": "npx",
      "args": ["-y", "@cloudwerxlab/gpt-image-1-mcp"],
      "env": {
        "OPENAI_API_KEY": "${OPENAI_API_KEY}",
        "GPT_IMAGE_OUTPUT_DIR": "${CLAUDE_PROJECT_DIR:-.}/public/images/generated"
      }
    }
  }
}
